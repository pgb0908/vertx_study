# Vert.x 게이트웨이 학습 로드맵

Nginx/Kong을 대체할 사내 게이트웨이 프로토타입을 목표로, Vert.x 4.x의 핵심
패턴만 골라 실습 중심으로 익힌다. 리액티브 경험(RxJava/WebFlux)은 있으므로
개념 재설명 대신 Vert.x 고유의 실행 모델과 게이트웨이 아키텍처에 시간을
집중한다.

- **Vert.x**: 4.x (안정판)
- **빌드**: Gradle
- **배포 가정**: 단일 인스턴스
- **설정**: 파일 기반 + 파일시스템 watch로 무중단 반영
- **프로토콜**: HTTP/1.1 + HTTPS(TLS 종단)
- **기간**: 2주 집중, 하루 3시간 이상
- **결과물**: 동작하는 미니 게이트웨이 프로토타입 코드

## 학습 범위

### 포함
- 필터 체인 아키텍처 (on/off 가능한 모듈형 필터)
- 경로 기반 라우팅 + 라운드로빈 로드밸런싱
- 파일 기반 라우팅/업스트림 설정 + 무중단 hot-reload
- JWT 인증/인가 필터
- 레이트리미팅 필터
- 회로차단기 · 타임아웃 · 재시도
- TLS 종단 (HTTPS)

### 다음 단계로 미룸
- 관측성 — 로깅 / 메트릭 / 트레이싱
- WebSocket / gRPC 프록시
- 클러스터링 / 멀티 인스턴스 설정 동기화
- 동적 플러그인(스크립트/클래스로더) 로딩

## 10 Day Plan

### Day 1 — Vert.x 실행 모델 적응
RxJava/WebFlux 멘탈모델을 Vert.x의 Verticle · Event Loop · Context로 옮겨 붙인다.

- **개념**: Event Loop 스레드 모델, Verticle 배포, `Future`/`Promise` 합성(compose/map), 블로킹 코드 격리(executeBlocking)
- **모듈**: `vertx-core`, `vertx-web`
- **실습**: Router 기반 최소 HTTP 서버를 하나의 Verticle로 기동, 두세 개의 비동기 호출을 Future 체이닝으로 연결
- **체크**: Vert.x에서 절대 이벤트 루프를 블로킹하면 안 되는 이유를 스스로 설명할 수 있다.

### Day 2–3 — 리버스 프록시 코어
들어온 요청을 업스트림으로 그대로(스트리밍) 전달하는 프록시 골격을 만든다.

- **개념**: WebClient로 업스트림 호출, 요청/응답 바디 스트리밍(버퍼링 없이 pipe), 헤더 전파 및 hop-by-hop 헤더 제거, 커넥션 풀 옵션
- **모듈**: `vertx-web-client`
- **실습**: 로컬에 더미 업스트림 2개(Vert.x로 간단히) 띄우고, 게이트웨이가 그대로 프록시하는지 curl로 확인
- **체크**: 대용량 바디를 메모리에 풀버퍼링하지 않고 프록시할 수 있다.

### Day 3–4 — 라우팅 테이블 + 파일 설정
라우팅 규칙과 업스트림 서버 목록을 코드가 아닌 파일로 정의하고 파싱한다.

- **개념**: YAML/JSON 스키마 설계(경로 → 업스트림 그룹 → 필터 목록), 불변 RouteTable 객체 모델링, 라운드로빈 LB
- **모듈**: `vertx-config` 또는 Jackson YAML 직접 파싱, `vertx-core FileSystem`
- **실습**: `routes.yaml` 하나로 3개 이상의 경로 → 서로 다른 업스트림 그룹 매핑, 그룹 내 라운드로빈 확인
- **체크**: 설정 파일 하나만 보고 게이트웨이 전체 라우팅 동작을 예측할 수 있다.

### Day 4–5 — 필터 체인 아키텍처 (핵심)
Zuul 스타일 pre/route/post 필터를 Vert.x Handler 체인으로 재구성한다.

- **개념**: 공통 Filter 인터페이스 + RequestContext, 필터 순서(order) 정의, per-route 필터 on/off를 설정값으로 표현, 미리 등록된 필터 레지스트리에서 이름으로 조회
- **모듈**: `vertx-web`의 `Router.route().handler()` 체이닝
- **실습**: 로깅용 더미 필터 2개를 만들어 라우트 설정의 `filters: [a, b]` 목록으로 순서/on-off 제어
- **체크**: 새 필터를 "필터 인터페이스 구현 + 레지스트리 등록 + 설정에 이름 추가"만으로 붙일 수 있다.

### Day 5–6 — 무중단 Hot-Reload
설정 파일이 바뀌면 재시작 없이 라우팅/필터 상태를 통째로 교체한다.

- **개념**: 파일시스템 watch(WatchService 또는 Vert.x FileSystem 폴링), 불변 설정 객체 + `volatile` 참조 교체(원자적 스왑), 진행 중인 요청은 이전 설정으로 안전하게 마무리
- **모듈**: `java.nio.file.WatchService` 또는 `vertx.setPeriodic` 폴링
- **실습**: 서버 켠 채로 `routes.yaml` 수정 → 몇 초 내 라우팅 변경 확인 (트래픽 끊김 없이)
- **체크**: 리로드 도중 들어온 요청이 예외 없이 이전/이후 둘 중 하나의 설정으로 일관되게 처리된다.

### Day 6–8 — 인증 필터 (JWT)
필터 체인에 실제 비즈니스 로직을 하나 얹어본다.

- **개념**: JWT 서명 검증, 클레임 기반 인가, 인증 실패 시 401 응답 및 체인 중단(short-circuit)
- **모듈**: `vertx-auth-jwt`
- **실습**: 특정 라우트에만 `auth-jwt` 필터를 설정으로 켜고, 토큰 없이/유효하지 않게 요청 시 401 확인
- **체크**: 필터가 체인을 중단시키는 지점(short-circuit)이 다른 필터에 영향 없이 동작한다.

### Day 8–9 — 레이트리미팅 필터
클라이언트별 요청 폭주를 막는 필터를 추가한다 (단일 인스턴스라 로컬 상태로 충분).

- **개념**: 토큰 버킷 또는 슬라이딩 윈도우 알고리즘, 클라이언트 식별(IP/API Key), 429 응답 + Retry-After 헤더
- **모듈**: `vertx-core`의 `LocalMap` 기반 자체 구현
- **실습**: 초당 N회로 제한 걸고 부하 스크립트로 429 발생 확인
- **체크**: 레이트리밋 상태 저장소가 이벤트 루프를 블로킹하지 않는다.

### Day 9–10 — 회로차단기 · 타임아웃 · 재시도
업스트림 장애가 게이트웨이 전체로 번지지 않도록 회복탄력성 계층을 얹는다.

- **개념**: CircuitBreaker의 open/half-open/closed 상태 전이, 타임아웃 설정, 멱등 요청에 한정한 재시도 정책
- **모듈**: `vertx-circuit-breaker`
- **실습**: 더미 업스트림 하나를 일부러 느리게/에러나게 만들어 회로 open 전환과 fallback 응답 확인
- **체크**: 장애 업스트림 하나가 다른 정상 업스트림 트래픽에 영향을 주지 않는다.

### Day 10 — TLS 종단 + 통합 마무리
HTTPS로 앞단을 감싸고, 지금까지 만든 필터 체인 전체를 하나의 시나리오로 검증한다.

- **개념**: `HttpServerOptions.setSsl`, 자체 서명 인증서로 로컬 TLS 종단
- **모듈**: `vertx-core`의 `HttpServerOptions`
- **실습**: HTTPS로 요청 → 인증 → 레이트리밋 → 라우팅 → 회로차단기 전체 경로를 통과하는 end-to-end 시나리오 1개 완주
- **체크**: 프로토타입이 처음 합의한 범위(라우팅/필터체인/hot-reload/인증/레이트리밋/회로차단기/TLS)를 전부 만족한다.

## 학습 중 유의할 결정 사항

- 필터는 클래스로더/스크립트로 동적 로딩하지 않는다 — 미리 배포된 필터를 **설정으로 on/off**하는 것으로 충분하다는 합의.
- 단일 인스턴스 가정이므로 레이트리밋 상태를 로컬 메모리에 두어도 무방하다. 다중 인스턴스로 갈 때는 이 부분을 Redis 등 공유 저장소로 옮겨야 한다.
- 파일 기반 설정은 인스턴스가 하나일 때만 단순하다 — 나중에 여러 인스턴스로 확장하면 설정 배포 방식(공유 스토리지/외부 설정 서버)을 다시 논의해야 한다.
